{% extends 'base.html.twig' %}

{% block title %}Ré-initialisé votre mot de passe{% endblock %}

{% block body %}
    <h1>Ré-initialisé votre mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Ré-initialisé votre mot de passe</button>
    {{ form_end(resetForm) }}
{% endblock %}
